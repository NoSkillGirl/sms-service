# deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sms-service
spec:
  progressDeadlineSeconds: 100
  replicas: 2
  selector:
    matchLabels:
      app: sms-service
  template:
    metadata:
      labels:
        app: sms-service
        version: v1
    spec:
      containers:
        - image: ajoop/sms-service:v1
          imagePullPolicy: Always
          name: sms-service
          ports:
            - containerPort: 8081
